generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mysql"
  url      = env("DATABASE_URL")
}

model youtube_channel {
  id       Int    @id @default(autoincrement())
  password String
}
